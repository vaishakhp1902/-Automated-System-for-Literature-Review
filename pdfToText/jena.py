import os
import a

send = ""

for i in a.s:
	print(i)
	x = str(i)
	send = send + x + ":"
#print(send)
'''
cmd = "javac -cp "+".:./lib/*"+" integrate.java"

os.system(cmd)  # returns the exit code in unix


cmd = "java -cp "+".:./lib/*"+" integrate"

os.system(cmd)  # returns the exit code in unix
'''

